const fs=require('fs'); const html=fs.readFileSync('index.html', 'utf8'); const lines=html.split('\n'); let divCount=0; for(let i=0;i<lines.length;i++){ const line=lines[i]; const open = (line.match(/<div/gi) || []).length; const close = (line.match(/<\/div/gi) || []).length; divCount += open - close; if (line.includes('id=\"filters-card\"')) console.log('filters-card at', i, 'depth', divCount); if (line.includes('id=\"calculate-btn\"')) console.log('calculate-btn at', i, 'depth', divCount); if (line.includes('Right Column: Results Dashboard')) console.log('Right column at', i, 'depth', divCount); }
