const fs=require('fs'); const lines=fs.readFileSync('index.html', 'utf8').split('\n'); let d=0; for(let i=0;i<lines.length;i++){ let o=(lines[i].match(/<div/gi)||[]).length; let c=(lines[i].match(/<\/div/gi)||[]).length; d+=o-c; if(lines[i].includes('class=\"controls-column\"')) console.log('controls-column at', i, 'depth', d); if(lines[i].includes('id=\"filters-card\"')) console.log('filters-card at', i, 'depth', d); if(lines[i].includes('id=\"settings-card\"')) console.log('settings-card at', i, 'depth', d); }
